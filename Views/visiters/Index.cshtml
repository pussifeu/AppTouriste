@model IEnumerable<AppTourist.Models.visiter>

@{
    ViewBag.Title = "Index";
}

<h2>Listes des visites</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.site.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.visiteur.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateVisite)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.site.Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.visiteur.Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateVisite)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
